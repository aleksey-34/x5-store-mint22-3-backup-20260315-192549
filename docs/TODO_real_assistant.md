# TODO: Режим «Реальный ассистент»

> Обновлено: 2026-03-17. Отмечено фактическое состояние по коду.

## Цель
Сделать ассистента, который не только отвечает, но и выполняет задачи по документообороту объекта с контролем результата.

## Ближайший бэклог

- [x] **БД заполнена из файловой системы** — создан `scripts/seed_db_from_filesystem.py`;
      импортировано 21 сотрудник (тип `employee`) + 22 приказ/наряд (типы `order`, `permit`, `order_register`).
      Повторный запуск идемпотентен.

- [~] Сценарий «полный цикл сотрудника»: создание карточки, приказов и сопроводительных документов
      по одной команде.
      _Частично: `_try_execute_assistant_scenario()` + `_execute_employee_documents_command()` в `arm_admin.py`.
      Нет финального шага: проверки что все файлы созданы и записаны в БД._

- [~] Команда «распечатай» с браузерным предпросмотром по сформированным файлам.
      _Частично: `GET /arm/fs/print-preview` + батч-UI (`batchGenerateAllFilteredBtn`) есть.
      Нет автоматического подбора файлов по имени сотрудника из команды._

- [ ] Этапный индикатор хода ассистента (разбор команды → действия → итоговая проверка).
      _Не реализован._

- [~] Подтверждение перед опасными действиями (удаление, перезапись, пакетная переработка).
      _Частично: `window.confirm()` только перед удалением в `deleteSelectedManualReview()`.
      Нет серверного слоя подтверждения для перезаписи и пакетной генерации._

- [~] Журнал действий ассистента с привязкой к пользователю и времени.
      _Частично: таблица `journal_entries` (category / content / source / created_at) есть и работает.
      **Поле user_id/performed_by отсутствует** — нужно добавить миграцией._

- [x] Режим «план на день»: список задач из пробелов в чек-листах и статуса сканов.
      _Реализован: `GET /arm/todo/today` — возвращает задачи дня, работает._

- [~] Автопроверка результата после действия (файл создан, папка обновлена, печатная форма доступна).
      _Частично: `db.rollback()` при ошибке сохранения файла есть. Универсального механизма нет._

- [x] Голосовые команды для бригадира/прораба.
      _Закрыто: нативный голосовой ввод Windows 11 работает отлично — отдельные шаблоны не нужны._

## Следующий этап

- [ ] «Память по объекту»: учитывать историю решений и ранее созданные документы.
      _Не реализовано. Можно опереться на `document_contents` + `journal_entries` как базу._

- [ ] Контроль качества ответов: полнота, точность ссылок на файлы, отсутствие артефактов.
      _Не реализовано._

- [~] Безопасный пакетный режим с пошаговым откатом.
      _Частично: `db.rollback()` на уровне одной операции. Полного транзакционного батча нет._

## Инфраструктура

- [x] Ollama/LocalLLM отключена (`LOCAL_LLM_ENABLED=false` в `.env`). Автостарт учитывает флаг.
- [x] БД SQLite: схема + данные. `documents` — 43 записи (21 employee + 22 order/permit).
- [x] API `GET /documents` возвращает все записи. Идемпотентный пересев: `scripts/seed_db_from_filesystem.py`.

## Статус Приказов (На 2026-03-17)

Проверка ниже теперь опирается не только на наличие файлов, но и на ручную сверку заголовков, назначения и общей формы ключевых приказов `ORDER_01`, `ORDER_02`, `ORDER_03`, `ORDER_05`, `ORDER_09`, `ORDER_10`, `ORDER_11`, `ORDER_14`, `ORDER_15`, `ORDER_16`.

- [x] 1) О назначении уполномоченного представителя лица, осуществляющего строительство.
- [~] 2) О допуске к СМР. (на доработке)
- [x] 3) Ответственное лицо: охрана труда.
- [x] 4) Ответственное лицо: пожарная безопасность.
- [x] 5) Ответственное лицо: погрузочно-разгрузочные работы.
- [x] 6) Ответственное лицо: промышленная безопасность (41).
- [x] 7) Ответственное лицо: подъемные сооружения (работа кранами).
- [~] 8) Сосуды под избыточным давлением (баллоны СУГ). (проверить форму и актуальность)
- [~] 9) Выдача и оформление нарядов-допусков для работ повышенной опасности. (на доработке)
- [~] 10) О назначении стропальщиков. (на доработке)
- [~] 11) О назначении ответственного за электрохозяйство. (на доработке)
- [x] 12) О стажировке рабочих. (не требуется)
- [x] 13) О допуске рабочих соответствующих профессий к работе и закреплении за ними участков. (не требуется)

## Приложения к приказам

- [~] 1.2) Приложить копии протоколов проверки знаний и удостоверений по направлениям
      на ответственных лиц из числа ИТР и рабочих. (собирается)
